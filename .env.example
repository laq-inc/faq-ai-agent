POSTGRES_DB=db
POSTGRES_USER=userp
POSTGRES_PASSWORD=password

DATABASE_URL=postgresql+psycopg://userp:password@db:5432/db

OPENAI_API_KEY=your_openai_api_key

# `claude setup-token` をホスト側で実行して取得したトークンを設定
CLAUDE_CODE_OAUTH_TOKEN=your_claude_code_oauth_token

# 初回のみ: コンテナ内で初期設定(テーマ選択/フォルダ信頼)ダイアログが
# 毎回出るのを防ぐため、以下を一度実行してください
#   cp .devcontainer/claude-home/claude.json.example .devcontainer/claude-home/claude.json

